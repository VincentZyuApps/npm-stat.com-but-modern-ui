{
  "name": "npm-stat-modern-ui",
  "version": "0.2.0-beta.5+20260916",
  "private": true,
  "description": "A modern Tampermonkey UI for npm-stat.com",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "check": "npm run typecheck && npm run test && npm run build && node scripts/verify-build.mjs && node --check dist/npm-stat-modern-ui.user.js"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.6",
    "vite-plugin-monkey": "^6.0.1"
  },
  "dependencies": {
    "echarts": "^6.1.0"
  }
}
